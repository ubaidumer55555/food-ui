// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F48E28", // Primary color
    },
    secondary: {
      main: "#FAFAFF", // Secondary color
    },
    text: {
      primary: "#000000", // Text color
    },
  },
});

export default theme;
